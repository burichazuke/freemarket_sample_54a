// 3つ目のページ
%main.single__page__main
  .single__page__main__container
    %h2.single__page__main__head
      住所入力
    = form_for resource, url: create_user_registration_path, html: {method: :post, class: "single__form"} do |f|
      .single__field
        .single__field__label
          = label :devise, :name, "お名前"
          %span.single__field__require 必須
        .single__field__input
          = text_field :devise, :first_name, autofocus: true, placeholder: "例) 山田"
        .single__field__input
          = text_field :devise, :last_name, placeholder: "例) 彩"
      .single__field
        .single__field__label
          = label :devise, :name_kana, "お名前カナ"
          %span.single__field__require 必須
        .single__field__input
          = text_field :devise, :first_name_kana, placeholder: "例) ヤマダ"
        .single__field__input
          = text_field :devise, :last_name_kana, placeholder: "例) アヤ"
      .single__field
        .single__field__label
          = label :address, :postal_code, "郵便番号"
          %span.single__field__require 必須
        .single__field__input
          = text_field :address, :postal_code, placeholder: "例) 123-4567"
      .single__field
        .single__field__label
          = label :address, :prefecture, "都道府県"
          %span.single__field__require 必須
        .single__field__select
          = select :address, :prefecture, placeholder: "例) 北海道"
      .single__field
        .single__field__label
          = label :address, :prefecture, "市区町村"
          %span.single__field__require 必須
        .single__field__input
          = text_field :address, :prefecture, placeholder: "例) 横浜市緑区"
      .single__field
        .single__field__label
          = label :address, :prefecture, "番地"
          %span.single__field__require 必須
        .single__field__input
          = text_field :address, :prefecture, placeholder: "例) 青山1-1-1"
      .single__field
        .single__field__label
          = label :address, :prefecture, "建物名"
          %span.single__field__require.single__field__require--option 任意
        .single__field__input
          = text_field :address, :prefecture, placeholder: "例) 柳ビル103"
      .single__field
        .single__field__label
          = label :address, :prefecture, "電話"
          %span.single__field__require.single__field__require--option 任意
        .single__field__input
          = text_field :address, :prefecture, placeholder: "例) 09012345678"          
      = f.submit "次へ進む", class: "single__form__send-btn"
