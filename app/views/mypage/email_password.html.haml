-breadcrumb :email_password
%main.mypage
  .mypage__container
    .mypage__contents
      .single__page__main
        .single__page__main__container
          %h2.single__page__main__head
            メール/パスワード
          = form_for current_user, url: update_user_registration_path, html: { method: :put, class: "single__form" } do |f|
            -# = devise_error_messages!
            .single__field
              .single__field__label
                = f.label :email, "メールアドレス"
                %span.single__field__require 必須
              .single__field__input
                = f.email_field :email, placeholder: "PC・携帯どちらでも可"

            .single__field
              .single__field__label
                = f.label :current_password, "現在のパスワード"
                %span.single__field__require 必須
              .single__field__input
                = f.password_field :current_password, placeholder: "6文字以上"

            .single__field
              .single__field__label
                = f.label :password, "新しいパスワード(確認)"
                %span.single__field__require 必須
              .single__field__input
                = f.password_field :password, placeholder: "6文字以上"
              
            .single__field
              .single__field__label
                = f.label :password_confirmation, "新しいパスワード(確認)"
                %span.single__field__require 必須
              .single__field__input
                = f.password_field :password_confirmation, placeholder: "6文字以上"
            .single__field
              %p.single__field__text
                パスワードを設定したい場合は上記をすべて入力してください。（設定しない場合は空欄）
            .single__field
              = f.submit "変更する", class: "single__form__send-btn"
    .mypage__side
      =render "shared/sidebar"