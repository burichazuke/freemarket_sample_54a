%header.single__page__header
  //メルカリのロゴの写真
  .logo
    = link_to "/" do
      ロゴ
    // = image_tag "", size: "185x50", alt: "mercari"
  - if controller_name == 'registrations' && action_name == 'new'
    %nav.sign-up__bar
      %ol.sign-up__bar__menus
        %li.active
          会員情報
          .progress-status
        %li
          電話番号認証
          .progress-status
        %li
          お届け先住所入力
          .progress-status
        %li
          支払い方法
          .progress-status
        %li
          完了
          .progress-status